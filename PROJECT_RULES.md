# PROJECT_RULES.md

## TASK 拆解

实现前先深度理解需求与使用场景，按模块拆解并输出一份完整 TASK 列表：

- 明确模块范围、交互目标、依赖关系、关键状态与交付顺序
- 目标：布局、样式与交互达到可直接交付状态，无需再做设计还原返工
- 其他开发者或 AI 智能体无需额外说明即可基于不同的技术栈继续完成功能逻辑、边界处理与后端对接
- 从根本上减少澄清成本与验收偏差

先与我确认共识，再逐一执行 TASK。

## Figma 还原

以 Figma 设计稿作为唯一视觉与交互基准：

- 优先通过 MCP 读取设计稿信息，尽可能高精度还原页面结构、组件状态、视觉样式与交互行为
- 禁止主观重构视觉方案
- 禁止硬编码设计变量。如项目中提供 `figma-variables-resolver.js`，则必须优先通过该文件获取对应变量
- 图标优先使用 SVG
- 最终效果应在样式、尺寸、布局、状态和交互上与设计稿保持一致
- 若设计稿图层命名无意义，需在理解界面语义后，主动为页面、模块、组件、状态与样式建立清晰、稳定、可维护的命名体系

## 暗黑模式

- 最大深度读取并理解 Light 模式现有配色，仅映射生成一套对应的 Dark 模式
- 只做颜色替换与对应关系调整
- 不新增元素、不新增变量、不修改布局、不改尺寸、不添加渐变

## 质量测试

优先使用 Playwright 等自动化方式执行 smoke test，覆盖以下方面：

- 核心功能、关键交互、UI 与设计稿一致性
- 主路径与高频操作
- 边界情况、异常状态、状态同步
- 构建启动流程、Demo 可用性
- 滚动、缩放、窗口变化、异步更新等复杂场景
- 控制台报错、警告与隐藏异常

必要时可先用临时测试脚本验证关键路径。测试后提供：通过项、发现的问题、建议修复优先级。

## 项目结构

遵循技术栈无关的模块化结构，保证可适配 React、Vue、Svelte、Flutter、SwiftUI 等不同技术栈：

```
project-name/
├── AGENTS.md           # AI 协作规则、代码输出规范、交付约束
├── Spec/               # （推荐）参考 spec-kit 沉淀规范与交付说明
├── public/             # 静态资源、无需编译的公共文件
└── src/
    ├── app/            # 应用入口、页面编排、Feature 挂载层
    ├── shared/         # 全局共享层
    │   ├── styles/     # 全局样式、Design Tokens、主题与 Reset
    │   ├── ui/         # 无业务耦合的通用 UI 组件
    │   ├── utils/      # 通用工具函数
    │   └── assets/     # 全局图标、图片、字体与动画资源
    └── features/       # 功能模块（独立、自包含、低耦合）
        └── feature-name/
            ├── components/   # 当前 Feature 内部 UI 组件
            ├── state/        # 当前 Feature 状态模型与交互状态
            ├── data/         # Mock 数据与演示状态数据
            ├── assets/       # 当前 Feature 私有资源
            └── FeatureDemo   # 当前 Feature 原型演示入口
```

如项目需沉淀设计规范，参见 https://github.com/github/spec-kit 建立 Spec 文件夹，集中沉淀组件参数、交互状态、设计变量、Design Tokens、资源清单与交付说明。

## 交付清理

- 检查项目完整性，仅保留与项目相关的必要内容
- 删除无关文件（尤其是可重新安装的依赖文件）
- 检查并更新 AGENTS.md
- 确保其他开发者或 AI 智能体无需额外说明即可正常使用、运行和维护当前所有项目与 Demo
